# Railway deployment configuration for PortfolioAI backend
# Deploy at: https://railway.app

[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 3

# Celery worker — add as a separate Railway service
# startCommand = "celery -A api.worker worker --loglevel=info --concurrency=2 -Q builds"
